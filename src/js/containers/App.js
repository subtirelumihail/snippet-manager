import 'styles/main.scss';

import React    from 'react';

export default React.createClass({
  render() {
    return (
      <div className="App">
      xxxxdasdasdas
        {this.props.children}
      </div>
    );
  }
});
